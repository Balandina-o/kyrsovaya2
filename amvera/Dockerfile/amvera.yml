meta:
  environment: jvm
  toolchain:
    name: maven
    version: 3.8.6
build:
  artifacts:
    target/*.war: /
run:
  jarName: /kyrsovaya2/artifacts/PropertyTaxWebApp.war
  persistence_mount: /data
  container_port: 80

